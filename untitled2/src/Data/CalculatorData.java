package Data;

import java.util.Stack;

public class CalculatorData {
    static Stack<Integer> bracket=new Stack<>();
    static void Check(String answer)
    {
        for(int i=0;i<answer.length();i++)
        {
            if(answer.charAt(i)=='('||answer.charAt(i)==')')
            {
                //
            }
        }
    }
}

